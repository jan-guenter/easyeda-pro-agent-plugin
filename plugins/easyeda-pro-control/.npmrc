# Install the signed, pinned dependency artifacts without running package hooks.
# esbuild uses its matching optional platform binary; explicit npm run commands
# still execute the reviewed project scripts.
ignore-scripts=true
